import styled from 'styled-components'

import TypographyMaterialUI from '@material-ui/core/Typography'

export const Typography = styled(TypographyMaterialUI)`
  margin-top: 25px;
  margin-bottom: 15px;
`
